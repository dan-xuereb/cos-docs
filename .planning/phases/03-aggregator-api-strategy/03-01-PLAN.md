---
phase: 03-aggregator-api-strategy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cos-docs/mkdocs.yml
  - cos-docs/requirements-docs.txt
  - cos-docs/docs/index.md
autonomous: true
requirements:
  - AGGR-01
  - AGGR-02
  - AGGR-04
  - AGGR-05
tags:
  - mkdocs
  - aggregator
  - monorepo-plugin
must_haves:
  truths:
    - "`mkdocs build --strict` from cos-docs/ exits 0 against the current (placeholder API) state of the 29 aggregated repos"
    - "Aggregator nav groups all 29 repos under exactly 8 domain headers"
    - "Aggregator uses `!include` (not `!import`); `search` + `monorepo` plugins active"
    - "Aggregator pins do NOT include mkdocstrings or griffe-pydantic (per-repo only)"
    - "Preflight verifies every !include target exists on disk before build"
  artifacts:
    - path: "cos-docs/mkdocs.yml"
      provides: "aggregator config with 8-domain nav + 29 !include entries + site_url + md_in_html"
      contains: "plugins:\n  - search\n  - monorepo"
    - path: "cos-docs/requirements-docs.txt"
      provides: "aggregator-only pinned deps (no mkdocstrings, no griffe-pydantic)"
    - path: "cos-docs/docs/index.md"
      provides: "placeholder overview so strict-build has nav target for `- Overview: index.md`"
  key_links:
    - from: "cos-docs/mkdocs.yml"
      to: "/home/btc/github/<repo>/mkdocs.yml (× 29)"
      via: "!include ../<repo>/mkdocs.yml"
      pattern: "!include \\.\\./[A-Za-z0-9_-]+/mkdocs\\.yml"
    - from: "cos-docs/requirements-docs.txt"
      to: "cos-docs/mkdocs.yml plugins list"
      via: "mkdocs-monorepo-plugin pinned version drives `- monorepo` activation"
---

<objective>
Create the aggregator `mkdocs.yml` + pinned `requirements-docs.txt` + a placeholder `docs/index.md` so that `mkdocs build --strict` from `cos-docs/` composes all 29 in-scope sibling repos under the 8 locked domain headers. This plan DOES NOT solve API rendering (that's 03-02) — declarative `::: <module>` directives in per-repo `docs/api.md` WILL be literal strings at aggregator build time per upstream mkdocs-monorepo-plugin issue #73. To get a green strict build at the end of THIS plan without Plan 03-02's pre-render in place, the placeholder `docs/index.md` is a minimal stub; per-repo `api.md` pages will render as literal directive text which is valid markdown (no strict-break). The end-of-plan gate is: `cd cos-docs && mkdocs build --strict` exits 0.

Purpose: Shipping the aggregator skeleton early (Wave 1) isolates the composition / nav / pin-matrix problem from the pre-render / content problems. This plan is self-contained; 03-02 and 03-03 layer onto the foundation it creates.

Output: `cos-docs/mkdocs.yml`, `cos-docs/requirements-docs.txt`, `cos-docs/docs/index.md` (placeholder), aggregator strict-build passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-aggregator-api-strategy/03-CONTEXT.md
@.planning/phases/03-aggregator-api-strategy/03-RESEARCH.md
@.planning/phases/03-aggregator-api-strategy/03-PATTERNS.md
@.planning/phases/02-content-migration/02-ROLLOUT-STATUS.md
@.planning/phases/01-scaffold-template/01-02-SUMMARY.md
@scripts/scaffold-all.sh

<interfaces>
# Per-repo mkdocs.yml canonical shape (the !include target — DO NOT modify any of these).
# From /home/btc/github/COS-Core/mkdocs.yml:

```yaml
site_name: COS-Core
theme:
  name: material
  features:
    - navigation.instant
    - navigation.tracking
    - content.code.copy
    - search.suggest
nav:
  - Overview: index.md
  - Architecture: architecture.md
  - API: api.md
plugins:
  - search
  - mkdocstrings:                # ← IGNORED by aggregator per mkdocs-monorepo-plugin issue #73
      handlers: ...
markdown_extensions:
  - admonition
  - pymdownx.details
  - pymdownx.superfences: ...
```

# The aggregator's `site_name` (child value) becomes the URL slug under aggregator `site/`.
# Example: child `site_name: COS-Core` → aggregated URL path `/COS-Core/`.

# 29-repo in-scope list for aggregator nav (PATTERNS.md domain grouping — final, audit-verified):
#
#   Forges (8):         bis-forge, bls-forge, BTC-Forge, EDGAR-Forge, FRED-Forge, imf-forge, ingest, stooq-forge
#   Signal Stack (6):   COS-BTE, COS-CIE, COS-MSE, COS-SGL, cos-signal-bridge, cos-signal-explorer
#   Agent (2):          COS-Bitcoin-Protocol-Intelligence-Platform, COS-LangGraph
#   Presentation (2):   cos-webpage, quant-dashboard
#   Warehouse (3):      COS-BTC-Node, COS-BTC-SQL-Warehouse, coinbase_websocket_BTC_pricefeed
#   Network (2):        COS-BTC-Network-Crawler, OrbWeaver
#   Schema (2):         COS-Core, cos-data-access
#   Infrastructure (4): COS-Capability-Gated-Agent-Architecture, COS-Hardware, COS-Infra, COS-Network
#   Total:              29
#
# EXCLUDED (locked, NOT in nav):
#   COS-electrs (Rust/Cargo — D-14 Phase 2)
#   capability-gated-agent-architecture (lowercase dup — D-12 Phase 2)
#   cos-docs (aggregator self-reference)
#   quant-dashboard-k8s-deployment (mentioned in CLAUDE.md but NOT on disk — verified 2026-04-19 via `ls`; A3 assumption resolved = drop from nav)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Preflight — verify all 29 !include target mkdocs.yml files exist on disk</name>
  <files>(read-only; no writes)</files>
  <read_first>
    - /home/btc/github/cos-docs/.planning/phases/02-content-migration/02-ROLLOUT-STATUS.md (confirm 26 OK / 2 FAIL(BTC-Forge,COS-MSE) / 2 SKIP(cos-docs, capability-gated-agent-architecture))
    - /home/btc/github/cos-docs/.planning/phases/02-content-migration/02-03-SUMMARY.md (confirm plan 02-03 shipped — `git log --oneline` should show `40d387f docs(02-03): complete per-repo content authoring plan`)
    - /home/btc/github/cos-docs/scripts/scaffold-all.sh (lines 43-89 — the authoritative ROLLOUT_LIST)
    - /home/btc/github/cos-docs/.planning/phases/03-aggregator-api-strategy/03-RESEARCH.md §"Common Pitfalls" Pitfall 3 and §"Environment Availability"
    - /home/btc/github/cos-docs/.planning/phases/03-aggregator-api-strategy/03-PATTERNS.md §"Primary READ Surfaces" → "For plan 03-01"
  </read_first>
  <action>
Run this preflight check verbatim and capture output. Every entry MUST exist; if ANY does not, ABORT this plan and report missing paths to the user before writing any aggregator files.

```bash
# Preflight: all 29 in-scope sibling mkdocs.yml files must exist.
# This is the D-18 "every repo must be !include-able at strict-build time" gate.

REPOS=(
  # Forges (8)
  bis-forge bls-forge BTC-Forge EDGAR-Forge FRED-Forge imf-forge ingest stooq-forge
  # Signal Stack (6)
  COS-BTE COS-CIE COS-MSE COS-SGL cos-signal-bridge cos-signal-explorer
  # Agent (2)
  COS-Bitcoin-Protocol-Intelligence-Platform COS-LangGraph
  # Presentation (2)
  cos-webpage quant-dashboard
  # Warehouse (3)
  COS-BTC-Node COS-BTC-SQL-Warehouse coinbase_websocket_BTC_pricefeed
  # Network (2)
  COS-BTC-Network-Crawler OrbWeaver
  # Schema (2)
  COS-Core cos-data-access
  # Infrastructure (4)
  COS-Capability-Gated-Agent-Architecture COS-Hardware COS-Infra COS-Network
)
MISSING=()
for r in "${REPOS[@]}"; do
  if [ ! -f "/home/btc/github/$r/mkdocs.yml" ]; then MISSING+=("$r"); fi
done
if [ "${#REPOS[@]}" -ne 29 ]; then
  echo "FATAL: REPOS list has ${#REPOS[@]} entries; expected 29." >&2; exit 2
fi
if [ "${#MISSING[@]}" -gt 0 ]; then
  echo "FATAL: missing mkdocs.yml for: ${MISSING[*]}" >&2; exit 3
fi
echo "OK: 29/29 !include targets present."
```

Also verify the strict-build status of the 2 FAIL repos from 02-ROLLOUT-STATUS.md: DO NOT try to fix them here (03-02 Wave 0 handles that). Just document their current state by running `ls /home/btc/github/BTC-Forge/mkdocs.yml /home/btc/github/COS-MSE/mkdocs.yml` — both files SHOULD exist (their per-repo mkdocs.yml is scaffolded; only their API docstring content breaks `--strict`). This plan's aggregator-level strict build will still PASS because at this point per-repo `docs/api.md` contains literal `::: <module>` text (markdown-valid; renders as unformatted string, no missing-anchor warning).

If preflight passes, proceed to Task 2. If it fails, STOP and report to user.
  </action>
  <verify>
    <automated>bash -c 'REPOS=(bis-forge bls-forge BTC-Forge EDGAR-Forge FRED-Forge imf-forge ingest stooq-forge COS-BTE COS-CIE COS-MSE COS-SGL cos-signal-bridge cos-signal-explorer COS-Bitcoin-Protocol-Intelligence-Platform COS-LangGraph cos-webpage quant-dashboard COS-BTC-Node COS-BTC-SQL-Warehouse coinbase_websocket_BTC_pricefeed COS-BTC-Network-Crawler OrbWeaver COS-Core cos-data-access COS-Capability-Gated-Agent-Architecture COS-Hardware COS-Infra COS-Network); [ "${#REPOS[@]}" -eq 29 ] || exit 2; for r in "${REPOS[@]}"; do [ -f "/home/btc/github/$r/mkdocs.yml" ] || { echo "MISSING: $r"; exit 3; }; done; echo OK'</automated>
  </verify>
  <acceptance_criteria>
    - Exactly 29 entries in REPOS list
    - Every `/home/btc/github/<repo>/mkdocs.yml` resolves to an existing file
    - Preflight script exits 0 with output ending "OK: 29/29 !include targets present."
    - `quant-dashboard-k8s-deployment` is NOT in the REPOS list (verified absent on disk 2026-04-19)
  </acceptance_criteria>
  <done>Preflight output captured; 29/29 targets verified; planner records count in execution log. No files modified.</done>
</task>

<task type="auto">
  <name>Task 2: Write cos-docs/requirements-docs.txt (aggregator pins — no mkdocstrings)</name>
  <files>cos-docs/requirements-docs.txt</files>
  <read_first>
    - /home/btc/github/COS-Core/requirements-docs.txt (per-repo analog — 4 lines; aggregator DROPS mkdocstrings + griffe-pydantic)
    - /home/btc/github/cos-docs/.planning/phases/03-aggregator-api-strategy/03-RESEARCH.md §"Standard Stack" (pin verification 2026-04-19)
    - /home/btc/github/cos-docs/.planning/phases/03-aggregator-api-strategy/03-PATTERNS.md §"cos-docs/requirements-docs.txt"
  </read_first>
  <action>
Create `cos-docs/requirements-docs.txt` with EXACTLY these 4 pinned lines (verbatim — verified on PyPI 2026-04-19; do NOT add anything else; do NOT include mkdocstrings or griffe-pydantic — those run ONLY in per-repo .venv-docs via 03-02's build-all-api.sh):

```
mkdocs==1.6.1
mkdocs-material==9.7.6
mkdocs-monorepo-plugin==1.1.2
pymdown-extensions>=10.9
```

Use atomic write (`tmp → rename`) per workspace convention. Absolute path: `/home/btc/github/cos-docs/requirements-docs.txt`. If the file already exists and matches these 4 lines exactly, leave it; otherwise overwrite atomically.

Rationale (record as inline comment at top of file):
```
# cos-docs aggregator pins. DELIBERATELY omits mkdocstrings[python] and
# griffe-pydantic — those run ONLY in per-repo .venv-docs via
# scripts/build-all-api.sh (Plan 03-02). Upstream mkdocs-monorepo-plugin
# issue #73: child plugins: blocks are NOT executed by parent build, so
# loading mkdocstrings into the aggregator venv is pointless and would
# require mega-venv of all 20 Python repos (rejected per D-01, evidence
# in 02-02-SUMMARY.md).
```

Then run `pip install -r /home/btc/github/cos-docs/requirements-docs.txt` in a fresh venv (or `uv pip install -r requirements-docs.txt --python .venv-aggr/bin/python` after `uv venv .venv-aggr`) to confirm all four pins resolve cleanly. Destroy the venv after the check.
  </action>
  <verify>
    <automated>grep -c '^mkdocs==1.6.1\|^mkdocs-material==9.7.6\|^mkdocs-monorepo-plugin==1.1.2\|^pymdown-extensions' /home/btc/github/cos-docs/requirements-docs.txt | grep -q '^4$'</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `/home/btc/github/cos-docs/requirements-docs.txt`
    - Contains exactly these 4 pin lines: `mkdocs==1.6.1`, `mkdocs-material==9.7.6`, `mkdocs-monorepo-plugin==1.1.2`, `pymdown-extensions>=10.9`
    - Does NOT contain `mkdocstrings` (zero occurrences): `! grep -q mkdocstrings /home/btc/github/cos-docs/requirements-docs.txt`
    - Does NOT contain `griffe-pydantic`: `! grep -q griffe-pydantic /home/btc/github/cos-docs/requirements-docs.txt`
    - `pip install -r` in fresh venv resolves all 4 pins with no conflicts
  </acceptance_criteria>
  <done>Aggregator requirements-docs.txt exists with exactly 4 pins; pip install dry-run confirms resolvability.</done>
</task>

<task type="auto">
  <name>Task 3: Write cos-docs/mkdocs.yml + placeholder docs/index.md, then run strict-build gate</name>
  <files>cos-docs/mkdocs.yml, cos-docs/docs/index.md</files>
  <read_first>
    - /home/btc/github/COS-Core/mkdocs.yml (theme + features + markdown_extensions verbatim donor)
    - /home/btc/github/cos-docs/.planning/phases/03-aggregator-api-strategy/03-RESEARCH.md §"Pattern 3" (the full 30-entry nav block — use the 29-entry version below, NOT the research draft's 30; quant-dashboard-k8s-deployment confirmed absent 2026-04-19)
    - /home/btc/github/cos-docs/.planning/phases/03-aggregator-api-strategy/03-RESEARCH.md §"Common Pitfalls" Pitfalls 6, 7, 8 (site_url, absolute links, theme features parity)
    - /home/btc/github/cos-docs/.planning/phases/03-aggregator-api-strategy/03-PATTERNS.md §"cos-docs/mkdocs.yml"
  </read_first>
  <action>
Step A — write `/home/btc/github/cos-docs/mkdocs.yml` atomically (tmp → rename) with EXACTLY this content (copy verbatim; values are pre-computed and audit-verified):

```yaml
site_name: Xuer Capital Workspace Docs
site_url: http://10.70.0.102:30081/
site_description: COS / Xuer Capital workspace documentation aggregator

# Aggregator for 29 sibling repos under /home/btc/github/. Each repo
# remains an independent git repo with its own docs/ tree and mkdocs.yml;
# mkdocs-monorepo-plugin composes them via !include at build time.
#
# IMPORTANT: child plugins: blocks are NOT executed by this aggregator build
# (upstream mkdocs-monorepo-plugin issue #73). Per-repo API rendering via
# mkdocstrings happens in per-repo .venv-docs under scripts/build-all-api.sh
# (Plan 03-02). That script pre-renders each Python repo's API page into an
# HTML passthrough before this aggregator build consumes the result.

theme:
  name: material
  features:
    - navigation.instant
    - navigation.tracking
    - navigation.sections
    - navigation.expand
    - content.code.copy
    - search.suggest
  palette:
    - scheme: default
    - scheme: slate

plugins:
  - search
  - monorepo
  # mkdocstrings intentionally NOT loaded at aggregator level — see note above.

markdown_extensions:
  - admonition
  - md_in_html
  - pymdownx.details
  - pymdownx.superfences:
      custom_fences:
        - name: mermaid
          class: mermaid
          format: !!python/name:pymdownx.superfences.fence_code_format

nav:
  - Overview: index.md
  - Forges:
      - bis-forge: '!include ../bis-forge/mkdocs.yml'
      - bls-forge: '!include ../bls-forge/mkdocs.yml'
      - BTC-Forge: '!include ../BTC-Forge/mkdocs.yml'
      - EDGAR-Forge: '!include ../EDGAR-Forge/mkdocs.yml'
      - FRED-Forge: '!include ../FRED-Forge/mkdocs.yml'
      - imf-forge: '!include ../imf-forge/mkdocs.yml'
      - ingest: '!include ../ingest/mkdocs.yml'
      - stooq-forge: '!include ../stooq-forge/mkdocs.yml'
  - Signal Stack:
      - COS-BTE: '!include ../COS-BTE/mkdocs.yml'
      - COS-CIE: '!include ../COS-CIE/mkdocs.yml'
      - COS-MSE: '!include ../COS-MSE/mkdocs.yml'
      - COS-SGL: '!include ../COS-SGL/mkdocs.yml'
      - cos-signal-bridge: '!include ../cos-signal-bridge/mkdocs.yml'
      - cos-signal-explorer: '!include ../cos-signal-explorer/mkdocs.yml'
  - Agent:
      - COS-Bitcoin-Protocol-Intelligence-Platform: '!include ../COS-Bitcoin-Protocol-Intelligence-Platform/mkdocs.yml'
      - COS-LangGraph: '!include ../COS-LangGraph/mkdocs.yml'
  - Presentation:
      - cos-webpage: '!include ../cos-webpage/mkdocs.yml'
      - quant-dashboard: '!include ../quant-dashboard/mkdocs.yml'
  - Warehouse:
      - coinbase_websocket_BTC_pricefeed: '!include ../coinbase_websocket_BTC_pricefeed/mkdocs.yml'
      - COS-BTC-Node: '!include ../COS-BTC-Node/mkdocs.yml'
      - COS-BTC-SQL-Warehouse: '!include ../COS-BTC-SQL-Warehouse/mkdocs.yml'
  - Network:
      - COS-BTC-Network-Crawler: '!include ../COS-BTC-Network-Crawler/mkdocs.yml'
      - OrbWeaver: '!include ../OrbWeaver/mkdocs.yml'
  - Schema:
      - COS-Core: '!include ../COS-Core/mkdocs.yml'
      - cos-data-access: '!include ../cos-data-access/mkdocs.yml'
  - Infrastructure:
      - COS-Capability-Gated-Agent-Architecture: '!include ../COS-Capability-Gated-Agent-Architecture/mkdocs.yml'
      - COS-Hardware: '!include ../COS-Hardware/mkdocs.yml'
      - COS-Infra: '!include ../COS-Infra/mkdocs.yml'
      - COS-Network: '!include ../COS-Network/mkdocs.yml'
```

Note deliberate NAV ordering: Forges alphabetical within; Warehouse alphabetical (coinbase_websocket < COS-BTC-Node < COS-BTC-SQL-Warehouse by byte-sort). Architecture page is omitted from nav in this plan — 03-03 will add `- Architecture: architecture.md` after authoring the workspace diagram. Do NOT add a nav entry to a file that doesn't exist yet (would break strict-build).

Step B — write a MINIMAL placeholder `/home/btc/github/cos-docs/docs/index.md` atomically (03-03 will replace with the full repo-index-table version):

```markdown
# Xuer Capital Workspace Docs

Aggregated documentation for all ~29 sibling repos under `/home/btc/github/`.

> This is a placeholder landing page. Plan 03-03 replaces it with the full
> repo-index table, quick-start, and domain overviews per D-11.

## Navigation

Use the left-nav to browse by domain: Forges, Signal Stack, Agent,
Presentation, Warehouse, Network, Schema, Infrastructure.
```

Step C — run the aggregator strict-build gate:

```bash
cd /home/btc/github/cos-docs
rm -rf .venv-aggr
uv venv --quiet .venv-aggr
uv pip install --quiet --python .venv-aggr/bin/python -r requirements-docs.txt
.venv-aggr/bin/mkdocs build --strict
```

Expected output: successful build, `site/` directory populated with 29 sub-dirs (one per `site_name`). If `--strict` fails with any "reference not found" or missing-anchor warning, investigate before declaring task complete. Common fail modes and fixes:
- "file does not exist" for a per-repo path → preflight (Task 1) should have caught this; re-run preflight.
- "unrecognized configuration" on a per-repo include → that child's mkdocs.yml has a syntax issue; defer to 03-02 (it will run per-repo strict builds).
- Missing-anchor in per-repo `api.md` → EXPECTED if that repo's api.md uses `::: <module>` directives — Material will render the directive as literal text. This is NOT a strict-build failure (no anchor referenced). If the build fails here specifically, document which repo and escalate.

Teardown: `rm -rf .venv-aggr` after a successful build (don't commit it).
  </action>
  <verify>
    <automated>cd /home/btc/github/cos-docs && rm -rf .venv-aggr site && uv venv --quiet .venv-aggr && uv pip install --quiet --python .venv-aggr/bin/python -r requirements-docs.txt && .venv-aggr/bin/mkdocs build --strict && [ -d site ] && ls site | wc -l | awk '{if ($1 < 25) exit 1}' && rm -rf .venv-aggr</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '!include \.\./' /home/btc/github/cos-docs/mkdocs.yml` returns exactly 29
    - `grep -c '^  - [A-Z]' /home/btc/github/cos-docs/mkdocs.yml` OR equivalent returns exactly 8 domain headers (Forges, Signal Stack, Agent, Presentation, Warehouse, Network, Schema, Infrastructure)
    - `grep -q 'site_url: http://10.70.0.102:30081/' /home/btc/github/cos-docs/mkdocs.yml`
    - `grep -q 'md_in_html' /home/btc/github/cos-docs/mkdocs.yml`
    - `grep -qE '^\s*-\s*monorepo\s*$' /home/btc/github/cos-docs/mkdocs.yml`
    - `grep -qE '^\s*-\s*search\s*$' /home/btc/github/cos-docs/mkdocs.yml`
    - `! grep -q mkdocstrings /home/btc/github/cos-docs/mkdocs.yml` (must NOT be in aggregator plugins)
    - `! grep -q quant-dashboard-k8s-deployment /home/btc/github/cos-docs/mkdocs.yml` (absent on disk)
    - `! grep -q COS-electrs /home/btc/github/cos-docs/mkdocs.yml` (D-14 exclusion)
    - `! grep -qE "!include \.\./capability-gated-agent-architecture/" /home/btc/github/cos-docs/mkdocs.yml` (D-12 lowercase dup excluded)
    - `/home/btc/github/cos-docs/docs/index.md` exists with H1 "# Xuer Capital Workspace Docs"
    - `mkdocs build --strict` from cos-docs/ exits 0
    - Resulting `site/` directory contains at least 29 subdirectories (one per `site_name`)
  </acceptance_criteria>
  <done>Aggregator mkdocs.yml + requirements-docs.txt + placeholder index.md all exist; strict-build gate passes; site/ contains 29+ subdirs. Plan 03-02 can now layer API pre-render on top.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI → aggregator venv | mkdocs / mkdocs-material / mkdocs-monorepo-plugin / pymdown-extensions pulled from PyPI into .venv-aggr |
| Sibling repos → aggregator build | 29 per-repo mkdocs.yml files included via `!include`; trusted (all under `/home/btc/github/` project control) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Tampering (supply-chain) | requirements-docs.txt PyPI pulls | mitigate | Exact `==` pins on mkdocs / mkdocs-material / mkdocs-monorepo-plugin; `>=` bound only on pymdown-extensions (transitive via Material). No floating deps. Version numbers verified via `pip index versions` 2026-04-19. |
| T-03-01-02 | Denial of Service (build-time) | `!include` resolution of 29 sibling paths | mitigate | Task 1 preflight verifies all 29 paths exist before build invocation; missing paths fail preflight with explicit list, not opaque mkdocs error. |
| T-03-01-03 | Information Disclosure | site_url exposes internal IP (10.70.0.102) | accept | Internal network only per PROJECT.md "Auth on the K8s site — internal-only network, no login wall needed"; no sensitive data in docs. |
| T-03-01-04 | Tampering (local build artifacts) | `.venv-aggr/`, `site/` directories | mitigate | Teardown at end of Task 3; `.gitignore` entry for `.venv-aggr/` + `site/` appended by 03-02 Task on gitignore hygiene. No commit of artifacts. |
| T-03-01-05 | Spoofing | Sibling repo `mkdocs.yml` points to unexpected docs_dir | accept | All 29 siblings are under project control; no external sources. If malicious content lands in a sibling repo, that is a parent-repo compromise, not an aggregator concern. |

All dispositions: **LOW severity**. Public static docs site with no auth / no user input / no DB / no API endpoints.
</threat_model>

<verification>
Manual smoke (optional; `mkdocs serve` at port 8000):
- `cd /home/btc/github/cos-docs && .venv-aggr/bin/mkdocs serve` (briefly, to eyeball the 8-domain left-nav structure). Kill with Ctrl-C. Not required for strict-build gate.

Automated gate (required):
- `mkdocs build --strict` from cos-docs/ must exit 0.
- `site/` directory must contain at least 29 subdirectories.
- Grep checks in Task 3 acceptance_criteria all must pass.
</verification>

<success_criteria>
- `cos-docs/mkdocs.yml` exists with 29 `!include` entries, 8 domain groups, `monorepo` + `search` plugins, `md_in_html`, `site_url` set
- `cos-docs/requirements-docs.txt` exists with exactly 4 pins, NO mkdocstrings
- `cos-docs/docs/index.md` exists as a placeholder
- `mkdocs build --strict` from cos-docs/ exits 0
- All 4 requirements (AGGR-01, AGGR-02, AGGR-04, AGGR-05) advanced (complete-pending-03-02-03-03 for full end-to-end verification)
</success_criteria>

<output>
After completion, create `.planning/phases/03-aggregator-api-strategy/03-01-SUMMARY.md` per the GSD summary template. Include: final !include count (29), 8-domain group audit, strict-build output tail, any preflight discrepancies (expected: none), teardown verification (no .venv-aggr/ or site/ committed).
</output>