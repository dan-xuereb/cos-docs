---
phase: 02-content-migration
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - /home/btc/github/bis-forge/docs/index.md
  - /home/btc/github/bis-forge/docs/architecture.md
  - /home/btc/github/bis-forge/docs/api.md
  - /home/btc/github/bls-forge/docs/index.md
  - /home/btc/github/bls-forge/docs/architecture.md
  - /home/btc/github/bls-forge/docs/api.md
  - /home/btc/github/BTC-Forge/docs/index.md
  - /home/btc/github/BTC-Forge/docs/architecture.md
  - /home/btc/github/BTC-Forge/docs/api.md
  - /home/btc/github/EDGAR-Forge/docs/index.md
  - /home/btc/github/EDGAR-Forge/docs/architecture.md
  - /home/btc/github/EDGAR-Forge/docs/api.md
  - /home/btc/github/FRED-Forge/docs/index.md
  - /home/btc/github/FRED-Forge/docs/architecture.md
  - /home/btc/github/FRED-Forge/docs/api.md
  - /home/btc/github/imf-forge/docs/index.md
  - /home/btc/github/imf-forge/docs/architecture.md
  - /home/btc/github/imf-forge/docs/api.md
  - /home/btc/github/stooq-forge/docs/index.md
  - /home/btc/github/stooq-forge/docs/architecture.md
  - /home/btc/github/stooq-forge/docs/api.md
  - /home/btc/github/ingest/docs/index.md
  - /home/btc/github/ingest/docs/architecture.md
  - /home/btc/github/ingest/docs/api.md
  - /home/btc/github/COS-Core/docs/index.md
  - /home/btc/github/COS-Core/docs/architecture.md
  - /home/btc/github/COS-Core/docs/api.md
  - /home/btc/github/COS-SGL/docs/index.md
  - /home/btc/github/COS-SGL/docs/architecture.md
  - /home/btc/github/COS-SGL/docs/api.md
  - /home/btc/github/COS-CIE/docs/index.md
  - /home/btc/github/COS-CIE/docs/architecture.md
  - /home/btc/github/COS-CIE/docs/api.md
  - /home/btc/github/cos-signal-bridge/docs/index.md
  - /home/btc/github/cos-signal-bridge/docs/architecture.md
  - /home/btc/github/cos-signal-bridge/docs/api.md
  - /home/btc/github/cos-signal-explorer/docs/index.md
  - /home/btc/github/cos-signal-explorer/docs/architecture.md
  - /home/btc/github/cos-signal-explorer/docs/api.md
  - /home/btc/github/COS-MSE/docs/index.md
  - /home/btc/github/COS-MSE/docs/architecture.md
  - /home/btc/github/COS-MSE/docs/api.md
  - /home/btc/github/COS-BTE/docs/index.md
  - /home/btc/github/COS-BTE/docs/architecture.md
  - /home/btc/github/COS-BTE/docs/api.md
  - /home/btc/github/COS-LangGraph/docs/index.md
  - /home/btc/github/COS-LangGraph/docs/architecture.md
  - /home/btc/github/COS-LangGraph/docs/api.md
  - /home/btc/github/quant-dashboard/docs/index.md
  - /home/btc/github/quant-dashboard/docs/architecture.md
  - /home/btc/github/cos-webpage/docs/index.md
  - /home/btc/github/cos-webpage/docs/architecture.md
  - /home/btc/github/coinbase_websocket_BTC_pricefeed/docs/index.md
  - /home/btc/github/coinbase_websocket_BTC_pricefeed/docs/architecture.md
  - /home/btc/github/COS-BTC-SQL-Warehouse/docs/index.md
  - /home/btc/github/COS-BTC-SQL-Warehouse/docs/architecture.md
  - /home/btc/github/COS-BTC-SQL-Warehouse/docs/api.md
  - /home/btc/github/COS-BTC-Network-Crawler/docs/index.md
  - /home/btc/github/COS-BTC-Network-Crawler/docs/architecture.md
  - /home/btc/github/COS-BTC-Network-Crawler/docs/api.md
  - /home/btc/github/OrbWeaver/docs/index.md
  - /home/btc/github/OrbWeaver/docs/architecture.md
  - /home/btc/github/COS-Bitcoin-Protocol-Intelligence-Platform/docs/index.md
  - /home/btc/github/COS-Bitcoin-Protocol-Intelligence-Platform/docs/architecture.md
  - /home/btc/github/COS-Bitcoin-Protocol-Intelligence-Platform/docs/api.md
  - /home/btc/github/COS-BTC-Node/docs/index.md
  - /home/btc/github/COS-BTC-Node/docs/architecture.md
  - /home/btc/github/cos-data-access/docs/index.md
  - /home/btc/github/cos-data-access/docs/architecture.md
  - /home/btc/github/cos-data-access/docs/api.md
  - /home/btc/github/COS-Hardware/docs/index.md
  - /home/btc/github/COS-Hardware/docs/architecture.md
  - /home/btc/github/COS-Network/docs/index.md
  - /home/btc/github/COS-Network/docs/architecture.md
  - /home/btc/github/COS-Infra/docs/index.md
  - /home/btc/github/COS-Infra/docs/architecture.md
  - /home/btc/github/COS-Capability-Gated-Agent-Architecture/docs/index.md
  - /home/btc/github/COS-Capability-Gated-Agent-Architecture/docs/architecture.md
autonomous: false
requirements: [CONT-02, CONT-03, CONT-04, DIAG-02]

must_haves:
  truths:
    - "For every non-exempt scaffolded repo, docs/index.md is hand-authored prose covering purpose + language/runtime + entry points + key commands (D-06, CONT-02)"
    - "For every non-exempt scaffolded repo, docs/architecture.md contains a hand-authored Mermaid diagram inside a ```mermaid fence + a written architecture overview (D-10, CONT-03, DIAG-02-amended)"
    - "For every Python scaffolded repo, docs/api.md contains the auto-generated `::: <pkg>` block PLUS a hand-curated list of important submodules rendered explicitly via `::: <pkg>.<submodule>` (D-07, CONT-04)"
    - "For each D-09 exempt repo (COS-Hardware, COS-Network, COS-Capability-Gated-Agent-Architecture), docs/index.md and docs/architecture.md are still hand-authored prose; the Mermaid diagram requirement is waived per D-08"
    - "Per-repo `mkdocs build --strict` (re-run in the existing .venv-docs) still passes after content edits"
    - "Per-repo content commit lands on the same branch (main or master) as the scaffold commit, with message `docs: populate index/architecture/api content`"
  artifacts:
    - path: "docs/index.md (per non-exempt repo)"
      provides: "Hand-authored Overview page replacing the Phase 1 stub"
      min_lines: 30
    - path: "docs/architecture.md (per non-exempt repo)"
      provides: "Hand-authored Mermaid diagram + prose overview"
      min_lines: 30
      contains: "```mermaid"
    - path: "docs/api.md (per Python repo only)"
      provides: "Auto `::: <pkg>` block + ≥ 2 hand-curated `::: <pkg>.<submodule>` blocks"
      min_lines: 15
      contains: ":::"
  key_links:
    - from: "docs/index.md"
      to: "the repo's README.md + CLAUDE.md"
      via: "human-authored summary distilling the source material per D-06"
      pattern: "purpose|entry|command"
    - from: "docs/architecture.md Mermaid block"
      to: "the repo's actual code structure"
      via: "diagram nodes match real modules / services / data flows"
      pattern: "```mermaid"
    - from: "docs/api.md submodule list"
      to: "the repo's `src/<pkg>/` tree"
      via: "every `::: <pkg>.<sub>` directive resolves to a real importable module"
      pattern: "::: "
---

<objective>
Hand-author per-repo `docs/index.md`, `docs/architecture.md`, and (for Python repos) `docs/api.md` content for every non-exempt sibling repo scaffolded in 02-02. This is the editorial pass that satisfies CONT-02, CONT-03, CONT-04, and the amended DIAG-02. Per D-05, this is human-authored prose drawn from each repo's existing `README.md` + `CLAUDE.md` — no LLM bulk-drafting.

Purpose: 02-01 built the tool, 02-02 ran it (creating stub-content commits across 30 repos). This plan replaces those stubs with real content so the docs aggregator (Phase 3) actually surfaces useful per-repo information.

Output: ~28 commits across the non-exempt repos (one per repo) of the form `docs: populate index/architecture/api content`. Plus 3 lighter commits for the D-09 exempt repos (index + prose architecture, no Mermaid).

Note on `autonomous: false`: D-05 explicitly assigns this work to the human maintainer. Tasks below structure the work into reviewable chunks; the executor's job is to (a) for each repo, surface the source material and a content checklist, then (b) WAIT for the user to author content, then (c) run the per-repo smoke gate + commit. Tasks may be done in any order — they have zero inter-dependency. Per CONTEXT.md "Specifics", per-repo authoring sessions are independent and can be interleaved with other work over a sustained editorial period.

**Phase completion gating note** (per 02-RESEARCH.md A5): the phase verifier should accept partial completion of this plan as long as CONT-02/03/04 are met for the in-scope-and-scaffolded subset. Repos still SKIP/FAIL from 02-02 do not block this plan; they block the parent phase only when the user de-scopes them or resolves them.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/btc/github/cos-docs/.planning/PROJECT.md
@/home/btc/github/cos-docs/.planning/REQUIREMENTS.md
@/home/btc/github/cos-docs/.planning/phases/02-content-migration/02-CONTEXT.md
@/home/btc/github/cos-docs/.planning/phases/02-content-migration/02-RESEARCH.md
@/home/btc/github/cos-docs/.planning/phases/02-content-migration/02-ROLLOUT-STATUS.md
@/home/btc/github/CLAUDE.md

<interfaces>
<!-- Per-repo content contract (D-06, D-07, D-10) — applies uniformly to every subtask below. -->

docs/index.md — REQUIRED sections (D-06, CONT-02):
  # <Repo Name>
  ## Purpose
    1-3 paragraph summary distilled from the repo's README intro.
  ## Language &amp; Runtime
    e.g. "Python 3.11+, FastAPI" — sourced from pyproject.toml / package.json.
  ## Entry Points
    e.g. "`python -m <pkg>`", "`<pkg>.cli`", "`uvicorn <module>:app`" — sourced from CLAUDE.md or `__main__.py` / `main.py`.
  ## Key Commands
    Install / run / test commands sourced from README install/run sections.

docs/architecture.md — REQUIRED sections (D-10, CONT-03):
  # Architecture
  ## Overview
    Written prose describing internal structure (modules, layers, data flow). Distill from CLAUDE.md where present.
  ## Diagram
    At least one ```mermaid fenced code block. Choose:
      - module/component graph (`graph TD` or `flowchart LR`) for libraries
      - data-flow diagram (`flowchart LR` with arrows showing data movement) for pipelines
      - sequence diagram (`sequenceDiagram`) for services/protocols
    Diagram nodes MUST correspond to actual code symbols (modules, services) — NOT placeholder A/B/C names.
  EXEMPT REPOS (D-09): COS-Hardware, COS-Network, COS-Capability-Gated-Agent-Architecture
    For these, docs/architecture.md still gets hand-authored prose, but the Mermaid block is OMITTED.

docs/api.md — REQUIRED structure (D-07, CONT-04, Python repos only):
  # API
  (intro paragraph from Phase 1 stub OK to keep)
  ::: &lt;pkg&gt;                    ← preserved from scaffold (Phase 1)
      options: ...
  ## &lt;Logical Group 1&gt;
  ::: &lt;pkg&gt;.&lt;submodule_a&gt;       ← hand-curated, sourced from CLAUDE.md module breakdown
  ::: &lt;pkg&gt;.&lt;submodule_b&gt;
  ## &lt;Logical Group 2&gt;
  ::: &lt;pkg&gt;.&lt;submodule_c&gt;

  Minimum: 2 explicit `::: <pkg>.<submodule>` blocks per Python repo.

Pre-flight per content-edit subtask:
  1. Read /home/btc/github/&lt;repo&gt;/README.md (always)
  2. Read /home/btc/github/&lt;repo&gt;/CLAUDE.md (when present — see 02-RESEARCH.md "Source-Material Readiness")
  3. Read /home/btc/github/&lt;repo&gt;/pyproject.toml (Python repos) for [project].name and dependencies
  4. ls /home/btc/github/&lt;repo&gt;/src/ (Python repos) to discover real submodule names

Per-repo verify gate after content edits:
  cd /home/btc/github/&lt;repo&gt; &amp;&amp; .venv-docs/bin/mkdocs build --strict
  (If .venv-docs missing — repo was committed in 02-02 but venv was cleaned — re-create:
   `uv venv .venv-docs &amp;&amp; uv pip install --python .venv-docs/bin/python -r requirements-docs.txt &amp;&amp; [pyproject.toml exists] &amp;&amp; uv pip install --python .venv-docs/bin/python -e .`)

Per-repo commit gate after build passes:
  git -C /home/btc/github/&lt;repo&gt; add docs/index.md docs/architecture.md [docs/api.md if Python]
  git -C /home/btc/github/&lt;repo&gt; commit -m "docs: populate index/architecture/api content"
  (do NOT git push)
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Domain group "Forges" — bis-forge, bls-forge, BTC-Forge, EDGAR-Forge, FRED-Forge, imf-forge, stooq-forge, ingest</name>
  <what-built>
    8 Python data-forge repos (7 forges + ingest reference connector). All Python, all get docs/api.md. None D-09 exempt; all need a Mermaid diagram. Domain grouping per /home/btc/github/CLAUDE.md "Data Forges" / "Data Ingestion Domain".
  </what-built>
  <read_first>
    For each repo R in {bis-forge, bls-forge, BTC-Forge, EDGAR-Forge, FRED-Forge, imf-forge, stooq-forge, ingest}:
    - /home/btc/github/R/README.md
    - /home/btc/github/R/CLAUDE.md (all 8 have one per 02-RESEARCH.md inventory; ingest is the exception — no CLAUDE.md, lean on README)
    - /home/btc/github/R/pyproject.toml ([project].name, dependencies)
    - ls /home/btc/github/R/src/ (or repo root) to discover real top-level package + submodules
  </read_first>
  <how-to-verify>
    Per repo R:
    1. **Author docs/index.md** per the index.md contract above. For forges: purpose = "Rate-limited downloader for &lt;source&gt;"; entry points typically `python -m &lt;pkg&gt;` or Flask app; key commands from README install/run sections.
    2. **Author docs/architecture.md.** Forges share a common pattern (per /home/btc/github/CLAUDE.md "Data Forges" section): rate-limited downloader → atomic Parquet/JSON.gz writes → SHA-256 manifests → Flask/FastAPI status API → K8s CronJob. Use a `flowchart LR` Mermaid diagram with nodes named after the actual code modules (read `src/&lt;pkg&gt;/` to confirm names — e.g., `core/`, `api/`, `cli/`).
    3. **Author docs/api.md.** Keep the auto `::: &lt;pkg&gt;` block. Add ≥ 2 explicit submodule blocks. Common forge submodules to consider: `::: &lt;pkg&gt;.core.downloader`, `::: &lt;pkg&gt;.core.manifest`, `::: &lt;pkg&gt;.api`, `::: &lt;pkg&gt;.cli`. Verify each `:::` target exists by `ls src/&lt;pkg&gt;/&lt;subpath&gt;`.
    4. **Per repo: re-run smoke gate** (cd into repo, `.venv-docs/bin/mkdocs build --strict`). Re-create venv if missing.
    5. **Per repo: commit** `git add docs/index.md docs/architecture.md docs/api.md && git commit -m "docs: populate index/architecture/api content"`.
    6. After all 8 repos done, `for r in bis-forge bls-forge BTC-Forge EDGAR-Forge FRED-Forge imf-forge stooq-forge ingest; do echo "=== $r ==="; head -5 /home/btc/github/$r/docs/index.md; grep -c '^:::' /home/btc/github/$r/docs/api.md; grep -q '```mermaid' /home/btc/github/$r/docs/architecture.md && echo "diagram OK" || echo "DIAGRAM MISSING"; done` — every repo: index.md non-stub, api.md has ≥ 3 ::: lines (1 auto + 2 curated), architecture.md has mermaid block.

    **Acceptance criteria (grep-verifiable):**
    - For each R: `wc -l /home/btc/github/R/docs/index.md` ≥ 30
    - For each R: `grep -q '```mermaid' /home/btc/github/R/docs/architecture.md`
    - For each R: `grep -c '^:::' /home/btc/github/R/docs/api.md` ≥ 3
    - For each R: `git -C /home/btc/github/R log --oneline -1 | grep -q 'populate index/architecture/api content'`
    - For each R: re-running `.venv-docs/bin/mkdocs build --strict` exits 0
    - None of the 8 should retain Phase 1 stub strings: `! grep -q "Replace this with" /home/btc/github/R/docs/{index,architecture}.md`
  </how-to-verify>
  <resume-signal>Type "approved" once all 8 forge-domain repos have content commits + build passing, OR list which repos are partially done and why.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Domain group "Schema + Signal Stack" — COS-Core, COS-SGL, COS-CIE, cos-signal-bridge, cos-signal-explorer, COS-MSE</name>
  <what-built>
    6 Python library repos forming the signal-discovery + signal-generation + composite-indicator stack. All Python, all get docs/api.md, all have Pydantic-heavy schemas (api.md is the highest-value page for these). None D-09 exempt.
  </what-built>
  <read_first>
    For each repo R in {COS-Core, COS-SGL, COS-CIE, cos-signal-bridge, cos-signal-explorer, COS-MSE}:
    - /home/btc/github/R/README.md
    - /home/btc/github/R/CLAUDE.md (all have one except cos-signal-bridge per 02-RESEARCH.md inventory)
    - /home/btc/github/R/pyproject.toml
    - ls /home/btc/github/R/src/&lt;pkg&gt;/ (e.g., COS-Core has cos_core/{models,adapters,provenance.py,types.py})
    - For COS-Core specifically: /home/btc/github/COS-Core/src/cos_core/models/market.py (this was the Phase 1 verification target — its OHLCVBar Pydantic model is the visual benchmark for "what good api.md rendering looks like" per 02-CONTEXT.md "Specifics")
  </read_first>
  <how-to-verify>
    Per repo R:
    1. **docs/index.md** — These are libraries, not services: entry points = "import &lt;pkg&gt; in another project". Pull purpose from README + /home/btc/github/CLAUDE.md (the workspace-level CLAUDE.md has dedicated sections for COS-Core, COS-SGL, COS-CIE, cos-signal-bridge that distill the architecture).
    2. **docs/architecture.md** — Use `graph TD` (module hierarchy) for these library repos. Example for COS-Core (from /home/btc/github/CLAUDE.md "COS-Core (Canonical Schemas)"):
       ```mermaid
       graph TD
         COS_Core[cos_core] --> Models[models/]
         COS_Core --> Adapters[adapters/]
         COS_Core --> Provenance[provenance.py]
         COS_Core --> Types[types.py]
         Models --> Market[market.py: OHLCVBar]
         Models --> Macro[macro.py: MacroSeries]
         Models --> OnChain[onchain.py: OnChainMetric]
         Adapters --> BTCForge[btc_forge.py]
         Adapters --> FRED[fred.py]
       ```
       Match the diagram to each repo's actual `src/<pkg>/` tree.
    3. **docs/api.md** — Per D-07, this is where these repos shine. Curate **≥ 4 submodule blocks** for each (more than the 2 minimum, since these are library repos where the API surface IS the product). Reference the workspace CLAUDE.md "Key modules" sections for each — e.g.:
       - **COS-Core**: `::: cos_core.models.market`, `::: cos_core.models.macro`, `::: cos_core.models.onchain`, `::: cos_core.adapters.btc_forge`, `::: cos_core.provenance`
       - **COS-SGL**: `::: xuer_sgl.signal_frame`, `::: xuer_sgl.alignment`, `::: xuer_sgl.join`, `::: xuer_sgl.talib_adapter`, `::: xuer_sgl.loaders.btc_forge`
       - **COS-CIE**: `::: cos_cie.composite`, `::: cos_cie.library`, `::: cos_cie.models`, `::: cos_cie.polarity`, `::: cos_cie.types`
       - **cos-signal-bridge**: `::: cos_signal_bridge.registry`, `::: cos_signal_bridge.evaluator`, `::: cos_signal_bridge.adapter`, `::: cos_signal_bridge.provider`, `::: cos_signal_bridge.feedback`
       - **cos-signal-explorer**: read `src/cos_signal_explorer/` to discover actual modules
       - **COS-MSE**: package name is `market_sentiment_engine` per Failure Mode 1 — read `src/market_sentiment_engine/` to discover submodules
    4. **Per-repo smoke + commit** as in Task 1.
    5. **Special check for COS-Core**: After content edits, the rendered `site/api/index.html` should still contain the Pydantic field-docstring text `Lowercase exchange name` (the Phase 1 verification benchmark). Run `cd /home/btc/github/COS-Core && .venv-docs/bin/mkdocs build --strict && grep -q 'Lowercase exchange name' site/api/index.html && echo COS-Core_API_OK`.

    **Acceptance criteria (grep-verifiable):**
    - For each R: same gates as Task 1 (index ≥ 30 lines, mermaid block present, ≥ 3 ::: lines in api.md)
    - For each R: api.md ::: count is HIGHER (≥ 5 = 1 auto + 4 curated, per the "library repos shine here" guidance) — `grep -c '^:::' /home/btc/github/R/docs/api.md` ≥ 5
    - For COS-Core: built site contains `Lowercase exchange name` (regression-protects Phase 1 verification)
    - For COS-MSE: api.md uses `market_sentiment_engine` not `cos_mse` (verifies PACKAGE_OVERRIDES handling if applicable)
  </how-to-verify>
  <resume-signal>Type "approved" once all 6 schema/signal-stack repos have content commits + build passing.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Domain group "Services + Presentation + Real-time" — COS-LangGraph, COS-BTE, quant-dashboard, cos-webpage, coinbase_websocket_BTC_pricefeed</name>
  <what-built>
    5 service / UI / real-time repos. COS-LangGraph + COS-BTE are Python services (api.md). quant-dashboard + cos-webpage are TypeScript (no api.md). coinbase_websocket_BTC_pricefeed is docs-only (no pyproject, no package.json — per 02-RESEARCH.md inventory). All require Mermaid diagrams (none D-09 exempt).
  </what-built>
  <read_first>
    For each repo R in {COS-LangGraph, COS-BTE, quant-dashboard, cos-webpage, coinbase_websocket_BTC_pricefeed}:
    - /home/btc/github/R/README.md
    - /home/btc/github/R/CLAUDE.md (COS-LangGraph, COS-BTE, quant-dashboard, cos-webpage have one; coinbase_websocket_BTC_pricefeed does NOT — README only)
    - For Python R: /home/btc/github/R/pyproject.toml + `ls src/&lt;pkg&gt;/`
    - For COS-LangGraph specifically: confirm api.md uses `langgraph_agent` (per PACKAGE_OVERRIDES from 02-01); read /home/btc/github/COS-LangGraph/src/langgraph_agent/ for submodule names (graph.py, tools_pricefeed.py, mcp_server.py, etc.)
    - For quant-dashboard: /home/btc/github/quant-dashboard/src/widgets/ + src/services/ + src/hooks/ for the architecture diagram
  </read_first>
  <how-to-verify>
    Per repo R, per repo type:

    **COS-LangGraph (Python service):**
    - **index.md**: purpose = ReAct agent on K8s NodePort 30091; runtime = Python 3.11+ FastAPI + LangGraph; entry = `uvicorn langgraph_agent.main:app`; key commands from README + /home/btc/github/COS-LangGraph/deploy/apply.sh.
    - **architecture.md**: Both module graph AND a sequence/data-flow diagram for the agent loop (per D-10 "service repos: both"). Reference the workspace CLAUDE.md "AI Agent Domain" section for the reactor/tool-loop description.
    - **api.md**: Confirm `::: langgraph_agent` (NOT `::: cos_langgraph` — Failure Mode 1). Curated submodules: `::: langgraph_agent.graph`, `::: langgraph_agent.tools_pricefeed`, `::: langgraph_agent.tools_btc`, `::: langgraph_agent.mcp_server`, `::: langgraph_agent.circuit_breaker`.

    **COS-BTE (Python service):**
    - **index.md**: purpose = backtesting engine on NautilusTrader; entry = Flask `/api/backtest`.
    - **architecture.md**: data-flow diagram (OHLCV Parquet → strategy → BTE runner → HTML report). Reference workspace CLAUDE.md "Backtesting Domain".
    - **api.md**: Curated submodules: `::: cos_bte.strategies`, `::: cos_bte.runners.backtest`, `::: cos_bte.data.loaders`, `::: cos_bte.api`.

    **quant-dashboard (TypeScript — NO api.md):**
    - **index.md**: purpose = Bloomberg-style trading dashboard; runtime = React 18 + Vite; entry = `npm run dev`; key commands from package.json scripts.
    - **architecture.md**: Mermaid showing widget registry pattern (src/widgets/registry.tsx → individual widgets → hooks → services/websocket.ts → ws-bridge). Reference workspace CLAUDE.md "Presentation Domain" section.
    - **DO NOT create docs/api.md** — Phase 1 scaffold correctly omits it for TS repos.
    - Note: quant-dashboard had 33 dirty files at 02-02 sweep time — confirm with rollout status whether it was scaffolded; if SKIPPED, this subtask MAY be deferred or done after manual scaffold.

    **cos-webpage (TypeScript — NO api.md):**
    - Same pattern as quant-dashboard. Read README+CLAUDE.md to discover actual purpose.

    **coinbase_websocket_BTC_pricefeed (docs-only — NO api.md):**
    - **index.md**: purpose = Coinbase WS → ZMQ PUB:5555 publisher; runtime = Python 3.12-slim Docker; entry = `python publisher.py`; key commands from README. (Note: this repo lacks CLAUDE.md per 02-RESEARCH.md inventory — author from README + workspace CLAUDE.md "Real-Time Market Data Domain" section.)
    - **architecture.md**: data-flow Mermaid: Coinbase WS → publisher.py → ZMQ PUB → downstream subscribers (langgraph + ws_bridge).
    - Note: this repo was on `kubernetes` branch at sweep time per 02-RESEARCH.md — confirm rollout status; may need manual scaffold first.

    **Per repo: smoke + commit:**
    - For Python repos and docs-only: `cd /home/btc/github/R && .venv-docs/bin/mkdocs build --strict`
    - For TS repos: same (mkdocs builds Markdown, doesn't care about TS)
    - Commit each repo separately with `docs: populate index/architecture/api content` (or `docs: populate index/architecture content` for non-Python repos — adjust message to match what files were actually edited).

    **Acceptance criteria (grep-verifiable):**
    - For each Python R (COS-LangGraph, COS-BTE): `grep -c '^:::' /home/btc/github/R/docs/api.md` ≥ 4
    - For COS-LangGraph: `grep -q '^::: langgraph_agent' /home/btc/github/COS-LangGraph/docs/api.md` AND `! grep -q '^::: cos_langgraph' /home/btc/github/COS-LangGraph/docs/api.md`
    - For each TS / docs-only R: `! test -f /home/btc/github/R/docs/api.md` (Phase 1 correctly omitted it)
    - For each R (any type): `grep -q '```mermaid' /home/btc/github/R/docs/architecture.md` AND `wc -l /home/btc/github/R/docs/index.md` ≥ 30
    - Each repo has commit `docs: populate ...` on main/master
  </how-to-verify>
  <resume-signal>Type "approved" once all 5 services/presentation/real-time repos have content commits + build passing. If quant-dashboard, cos-webpage, or coinbase_websocket_BTC_pricefeed were SKIPPED in 02-02 (branch or dirty), note their status and they can be addressed after manual scaffolding.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Domain group "Warehouse + Network + Bitcoin" — COS-BTC-SQL-Warehouse, COS-BTC-Network-Crawler, OrbWeaver, COS-Bitcoin-Protocol-Intelligence-Platform, COS-BTC-Node, cos-data-access</name>
  <what-built>
    6 Bitcoin-domain repos: warehouse ETL, network crawlers, BIP intelligence, BTC node config, data access layer. Mix of Python services and docs-only. All require Mermaid diagrams (none D-09 exempt).
  </what-built>
  <read_first>
    For each repo R in {COS-BTC-SQL-Warehouse, COS-BTC-Network-Crawler, OrbWeaver, COS-Bitcoin-Protocol-Intelligence-Platform, COS-BTC-Node, cos-data-access}:
    - /home/btc/github/R/README.md
    - /home/btc/github/R/CLAUDE.md (all have one EXCEPT OrbWeaver per 02-RESEARCH.md inventory)
    - For Python R (warehouse, crawler, BPIP, cos-data-access): pyproject.toml + `ls src/&lt;pkg&gt;/`
    - COS-BTC-Node and OrbWeaver are docs-only (no pyproject)
    - **Pre-existing docs/ collisions** (per 02-RESEARCH.md): COS-BTC-SQL-Warehouse already has `docs/spec_v1.2.md` (135 KB) — note it but don't touch it.
  </read_first>
  <how-to-verify>
    Per repo R, similar pattern to prior tasks:

    **COS-BTC-SQL-Warehouse (Python service, has existing docs/):**
    - **index.md**: purpose = Bitcoin full-chain ETL into ClickHouse; entry = `python -m btc_warehouse.cli`; key commands from README. Add a footnote about the pre-existing `spec_v1.2.md` ("Detailed pipeline spec: [spec_v1.2.md](spec_v1.2.md) — not in default nav; see this file for the full data model").
    - **architecture.md**: data-flow Mermaid: Bitcoin RPC → Pass1 (blocks/transactions) → Pass2 (UTXO resolution) → Pass3 (MetricRegistry) → ClickHouse → FastAPI/Redis serving. Reference workspace CLAUDE.md "On-Chain Analytics Domain".
    - **api.md**: `::: btc_warehouse`, `::: btc_warehouse.metrics.registry`, `::: btc_warehouse.pipeline.pass1`, `::: btc_warehouse.pipeline.pass2`, `::: btc_warehouse.api` (verify against actual `src/btc_warehouse/` tree).

    **COS-BTC-Network-Crawler (Python — package may be `crawl_arch` per Failure Mode 1):**
    - Verify importable module name: `ls /home/btc/github/COS-BTC-Network-Crawler/src/`. If the actual top-level module differs from `crawl_arch`, this is a NEW PACKAGE_OVERRIDES candidate — flag in summary and backport into scaffold-all.sh as part of this subtask's wrap-up.
    - **index/architecture/api**: standard pattern; reference workspace CLAUDE.md "Network Intelligence Domain".

    **OrbWeaver (docs-only on `kubernetes` branch — per 02-RESEARCH.md inventory):**
    - May have been SKIPPED in 02-02 (branch ≠ main + dirty). If so, this subtask cannot complete for OrbWeaver until manual scaffolding. Document status in this task's outcome.
    - If scaffolded: index.md from README; architecture.md hand-authored Mermaid (peer crawler topology).
    - No CLAUDE.md per 02-RESEARCH.md — README only.

    **COS-Bitcoin-Protocol-Intelligence-Platform (Python service, importable name = `bpip`):**
    - **index.md**: purpose = BIP lifecycle tracking → investment intelligence; entry = FastAPI on NodePort 31054.
    - **architecture.md**: Mermaid showing ingestion adapters (BIP repo, Optech, mailing lists) → entity extractor → PostgreSQL+TimescaleDB+Neo4j → FastAPI routers → React frontend. Reference workspace CLAUDE.md description.
    - **api.md**: `::: bpip`, `::: bpip.backend.ingestion`, `::: bpip.backend.api.main`, `::: bpip.entity_extractor` etc.

    **COS-BTC-Node (docs-only):**
    - index.md from CLAUDE.md (no README per 02-RESEARCH.md).
    - architecture.md: Mermaid showing Bitcoin Core → ZMQ rawblock → downstream consumers (warehouse, etc.).

    **cos-data-access (Python):**
    - Discover purpose from README + CLAUDE.md (this is one of the "added in D-14 reconciliation" repos).
    - Standard module + api pattern.

    **Acceptance criteria (grep-verifiable):**
    - Same structural gates as prior tasks (index ≥ 30 lines, architecture has mermaid, api has ≥ 3 ::: for Python repos)
    - For each repo: scaffold commit (from 02-02) + content commit (from this task) both present in `git log --oneline -5`
    - For COS-BTC-SQL-Warehouse: pre-existing `docs/spec_v1.2.md` is UNTOUCHED (`git -C /home/btc/github/COS-BTC-SQL-Warehouse log --follow docs/spec_v1.2.md` shows no new commit from this task)
    - For COS-BTC-Network-Crawler: if module name mismatch was discovered, the addition is recorded in the task summary AND backported into scaffold-all.sh PACKAGE_OVERRIDES
  </how-to-verify>
  <resume-signal>Type "approved" once all 6 warehouse/network/Bitcoin repos have content commits + build passing. Note any repos still SKIPPED from 02-02 (likely OrbWeaver per its branch state).</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Domain group "Infrastructure + Exempt" — COS-Hardware, COS-Network, COS-Infra, COS-Capability-Gated-Agent-Architecture</name>
  <what-built>
    4 docs/spec repos. THREE are D-09 diagram-exempt (COS-Hardware, COS-Network, COS-Capability-Gated-Agent-Architecture). COS-Infra is NOT exempt and DOES need a Mermaid diagram. All are docs-only (no api.md).
  </what-built>
  <read_first>
    For each repo R in {COS-Hardware, COS-Network, COS-Infra, COS-Capability-Gated-Agent-Architecture}:
    - /home/btc/github/R/README.md
    - /home/btc/github/R/CLAUDE.md (only COS-Infra has one per 02-RESEARCH.md inventory; the other 3 do NOT)
    - For COS-Infra: ls /home/btc/github/COS-Infra/docs/ — has 12 pre-existing deployment-guide .md files (per 02-RESEARCH.md Pitfall 8) that should NOT be touched
  </read_first>
  <how-to-verify>
    **COS-Hardware (D-09 exempt):**
    - **index.md**: purpose = server hardware specs + host inventory; runtime = N/A (docs); key "commands" = N/A or "browse the markdown files".
    - **architecture.md**: prose only (per D-08, NO Mermaid). Distill from README. May be a short page describing the inventory structure (host list, role mapping, etc.).
    - No CLAUDE.md to draw from — README only.

    **COS-Network (D-09 exempt):**
    - Same pattern as COS-Hardware. Prose only. Distill physical/VLAN/routing/firewall layer descriptions from README.

    **COS-Infra (NOT exempt — needs Mermaid):**
    - **index.md**: purpose = workspace deployment artifacts and runbooks (per /home/btc/github/CLAUDE.md added row from 02-02 Task 2C); reference the existing 12 deployment guides as further reading.
    - **architecture.md**: Mermaid showing the Talos cluster topology (control-plane node + namespaces + service mesh). Source from CLAUDE.md.
    - Add a footnote: "Detailed per-service deployment guides: see [deployment guides in docs/](.) — not all surfaced in the default nav. Edit `mkdocs.yml` to add specific guides as needed."
    - **DO NOT** modify the 12 pre-existing docs/*.md deployment guides — they are user-owned content from before scaffolding.

    **COS-Capability-Gated-Agent-Architecture (D-09 exempt):**
    - May still be on `spec-decomposition-extraction` branch / dirty per 02-RESEARCH.md inventory — likely SKIPPED in 02-02. Document status; if not scaffolded, this subtask deferred for this repo.
    - If scaffolded: index.md from README (no CLAUDE.md); architecture.md prose only describing the formal spec structure.

    **Per repo: smoke + commit** (docs-only repos build with mkdocs too — they just have a 2-page nav).

    **Acceptance criteria (grep-verifiable):**
    - For COS-Hardware, COS-Network, COS-Capability-Gated-Agent-Architecture: `! grep -q '```mermaid' /home/btc/github/R/docs/architecture.md` (no Mermaid — D-08 exempt)
    - For COS-Hardware, COS-Network, COS-Capability-Gated-Agent-Architecture: `wc -l /home/btc/github/R/docs/architecture.md` ≥ 20 (still has prose)
    - For COS-Infra: `grep -q '```mermaid' /home/btc/github/COS-Infra/docs/architecture.md` (NOT exempt — diagram required)
    - For COS-Infra: `ls /home/btc/github/COS-Infra/docs/*.md | wc -l` ≥ 14 (12 pre-existing + index.md + architecture.md)
    - For COS-Infra: `git -C /home/btc/github/COS-Infra log --oneline docs/` shows the scaffold commit + content commit but NO commits modifying the 12 pre-existing files
    - Each non-skipped repo has `docs: populate index/architecture content` commit on main/master
  </how-to-verify>
  <resume-signal>Type "approved" once all 4 infra/exempt repos have content commits + build passing. Note any repos still SKIPPED from 02-02 (likely COS-Capability-Gated-Agent-Architecture per its branch+dirty state).</resume-signal>
</task>

</tasks>

<verification>
**Plan-level checks (run after all 5 tasks complete):**

1. **Coverage check** — count repos with content vs scaffold-only:
   ```bash
   for r in $(grep -oE '/home/btc/github/[A-Za-z_-]+' /home/btc/github/cos-docs/.planning/phases/02-content-migration/02-03-PLAN.md | sort -u | sed 's|/home/btc/github/||'); do
     test -f /home/btc/github/$r/docs/index.md || continue
     if grep -q "Replace this with" /home/btc/github/$r/docs/index.md; then
       echo "$r: STUB (not authored)"
     else
       echo "$r: AUTHORED"
     fi
   done
   ```
   Expectation: ≥ 25 of 30 say AUTHORED.

2. **DIAG-02 satisfaction** — every non-exempt scaffolded repo has a Mermaid diagram:
   ```bash
   EXEMPT="COS-Hardware COS-Network COS-Capability-Gated-Agent-Architecture"
   for r in $(ls -d /home/btc/github/*/docs 2>/dev/null | xargs -n1 dirname | xargs -n1 basename); do
     test -f /home/btc/github/$r/docs/architecture.md || continue
     if echo "$EXEMPT" | grep -q "$r"; then continue; fi
     grep -q '```mermaid' /home/btc/github/$r/docs/architecture.md || echo "$r: MISSING DIAGRAM"
   done
   ```
   Expectation: zero output (every non-exempt repo has a diagram).

3. **CONT-04 satisfaction** — every Python repo has ≥ 3 ::: lines in api.md (1 auto + 2 curated):
   ```bash
   for r in bis-forge bls-forge BTC-Forge COS-Bitcoin-Protocol-Intelligence-Platform COS-BTC-Network-Crawler COS-BTC-SQL-Warehouse COS-BTE COS-CIE COS-Core cos-data-access COS-LangGraph COS-MSE COS-SGL cos-signal-bridge cos-signal-explorer EDGAR-Forge FRED-Forge imf-forge ingest stooq-forge; do
     test -f /home/btc/github/$r/docs/api.md || { echo "$r: NO_API_MD"; continue; }
     n=$(grep -c '^:::' /home/btc/github/$r/docs/api.md)
     [ "$n" -ge 3 ] && echo "$r: api.md has $n ::: blocks" || echo "$r: api.md has only $n ::: blocks (need ≥ 3)"
   done
   ```

4. **Phase 1 regression check** — COS-Core's Pydantic field rendering still works after content edits:
   ```bash
   cd /home/btc/github/COS-Core && .venv-docs/bin/mkdocs build --strict 2>&1 | tail -5
   grep -q 'Lowercase exchange name' /home/btc/github/COS-Core/site/api/index.html && echo COS-Core_API_OK
   ```

5. **Branch-policy check** — every content commit landed on main or master, not on a feature branch:
   ```bash
   for r in $(grep -oE '/home/btc/github/[A-Za-z_-]+' /home/btc/github/cos-docs/.planning/phases/02-content-migration/02-03-PLAN.md | sort -u | sed 's|/home/btc/github/||'); do
     test -d /home/btc/github/$r/.git || continue
     branch=$(git -C /home/btc/github/$r rev-parse --abbrev-ref HEAD)
     case "$branch" in main|master) ;; *) echo "$r: content commit on branch=$branch (should be main/master)";; esac
   done
   ```
</verification>

<success_criteria>
- For ≥ 25 of the 30 in-scope sibling repos: `docs/index.md`, `docs/architecture.md`, and (for Python repos) `docs/api.md` are hand-authored (no Phase 1 stub strings remain)
- DIAG-02 satisfied: every non-exempt scaffolded repo has a Mermaid block in `docs/architecture.md`
- D-09 exempt repos (COS-Hardware, COS-Network, COS-Capability-Gated-Agent-Architecture) have prose-only architecture pages (no Mermaid required)
- CONT-04 satisfied: every Python repo's `docs/api.md` has the auto `:::` block PLUS ≥ 2 hand-curated submodule blocks
- All content commits landed on main/master per D-02 (no commits to feature branches)
- Per-repo `mkdocs build --strict` continues to pass after content edits
- Phase 1 regression: COS-Core's `Lowercase exchange name` still appears in built `site/api/index.html`
- Any repos NOT authored are documented in this plan's SUMMARY with reason (e.g., still SKIPPED from 02-02 due to branch/dirty state)
</success_criteria>

<output>
After completion, create `/home/btc/github/cos-docs/.planning/phases/02-content-migration/02-03-SUMMARY.md` documenting:
- Final count of authored vs stub repos (target ≥ 25/30)
- Per-domain-group completion status (Forges, Schema/Signal Stack, Services/Presentation/Real-time, Warehouse/Network/Bitcoin, Infrastructure/Exempt)
- Any deferred repos (still in stub state) with reason
- Any new PACKAGE_OVERRIDES discovered during authoring (with backport status)
- Any per-repo decisions worth recording (e.g., "added a 4th nav entry to COS-BTC-SQL-Warehouse for spec_v1.2.md")
- Phase 2 completion confirmation: CONT-01, CONT-02, CONT-03, CONT-04, DIAG-02 (amended) all satisfied for the in-scope-and-completed subset
</output>
</content>
</invoke>