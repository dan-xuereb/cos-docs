---
phase: 02-content-migration
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - /home/btc/github/cos-docs/.planning/phases/02-content-migration/02-ROLLOUT-STATUS.md
  - /home/btc/github/cos-docs/.planning/REQUIREMENTS.md
  - /home/btc/github/CLAUDE.md
autonomous: false
requirements: [CONT-01, DIAG-02]

must_haves:
  truths:
    - "All 30 in-scope sibling repos have either (a) a 'docs: add cos-docs scaffold (content to follow)' commit on main/master OR (b) a documented skip reason in the rollout status report"
    - "Each scaffolded repo's `mkdocs build --strict` exits 0 in the smoke gate"
    - "REQUIREMENTS.md DIAG-02 wording is amended to acknowledge the D-09 exempt list"
    - "/home/btc/github/CLAUDE.md project map matches disk reality (D-14 reconciliation): renames applied, missing repos added, COS-electrs documented as out-of-stack"
  artifacts:
    - path: "/home/btc/github/cos-docs/.planning/phases/02-content-migration/02-ROLLOUT-STATUS.md"
      provides: "Per-repo rollout status table — OK / SKIP / FAIL with reason and remediation note for each of the 30 repos"
      min_lines: 50
      contains: "REPO"
    - path: "/home/btc/github/cos-docs/.planning/REQUIREMENTS.md"
      provides: "DIAG-02 amended to 'per non-exempt repo' with exempt list"
      contains: "non-exempt"
    - path: "/home/btc/github/CLAUDE.md"
      provides: "Workspace project map updated to match disk (renames, additions, COS-electrs note)"
      contains: "FRED-Forge"
  key_links:
    - from: "scaffold-all.sh sweep output"
      to: "02-ROLLOUT-STATUS.md"
      via: "operator transcribes wrapper summary into status report"
      pattern: "OK|SKIP|FAIL"
    - from: "DIAG-02 amendment"
      to: "scaffold-all.sh DIAGRAM_EXEMPT array"
      via: "requirements doc references the exempt list maintained in code"
      pattern: "COS-Hardware|COS-Network|COS-Capability-Gated-Agent-Architecture"
---

<objective>
Execute the Phase 2 mechanical rollout: run `scaffold-all.sh` against the 30 in-scope sibling repos, triage per-repo failures (dirty trees, branch mismatches, module-name mismatches), produce a per-repo status report, and apply the two planning-doc amendments that the rollout makes necessary (REQUIREMENTS.md DIAG-02 wording per D-08, `/home/btc/github/CLAUDE.md` project map per D-14).

Purpose: This is where Phase 2 actually touches sibling repos. Plan 02-01 built the tool; this plan applies it. Per CONTEXT.md "Specifics", the wrapper's job ends at scaffold + smoke + commit-of-stubs; hand-authoring (02-03) is decoupled and runs independently per repo afterward.

Output: 30 sibling repos each carrying a `docs: add cos-docs scaffold (content to follow)` commit (modulo documented skips), a per-repo status report in cos-docs/.planning/, an amended REQUIREMENTS.md, and a fixed workspace CLAUDE.md project map.

Note on `autonomous: false`: Per-repo failure triage requires human judgment (decide whether a dirty tree is safe to clean, whether a branch is safe to switch, whether a module-name mismatch needs a new PACKAGE_OVERRIDES entry). The rollout itself is mechanical, but the failure-handling loop is not.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/btc/github/cos-docs/.planning/PROJECT.md
@/home/btc/github/cos-docs/.planning/ROADMAP.md
@/home/btc/github/cos-docs/.planning/STATE.md
@/home/btc/github/cos-docs/.planning/REQUIREMENTS.md
@/home/btc/github/cos-docs/.planning/phases/02-content-migration/02-CONTEXT.md
@/home/btc/github/cos-docs/.planning/phases/02-content-migration/02-RESEARCH.md
@/home/btc/github/cos-docs/.planning/phases/02-content-migration/02-01-PLAN.md
@/home/btc/github/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run scaffold-all.sh full sweep + capture rollout status report</name>
  <files>
    /home/btc/github/cos-docs/.planning/phases/02-content-migration/02-ROLLOUT-STATUS.md
  </files>
  <read_first>
    - /home/btc/github/cos-docs/scripts/scaffold-all.sh (the tool from 02-01)
    - /home/btc/github/cos-docs/scripts/scaffold.sh (its --package flag)
    - /home/btc/github/cos-docs/.planning/phases/02-content-migration/02-RESEARCH.md §"Repo Inventory" (the 31-repo table — gives expected SKIP reasons for the 14 dirty repos and 4 non-main-branch repos)
  </read_first>
  <action>
    1. **Run the wrapper:**
       ```bash
       /home/btc/github/cos-docs/scripts/scaffold-all.sh 2&gt;&amp;1 | tee /tmp/scaffold-all.log
       ```
       Capture the printed summary AND note the `Per-repo logs:` directory path printed at the end.

    2. **Parse the summary into a status report.** Create `/home/btc/github/cos-docs/.planning/phases/02-content-migration/02-ROLLOUT-STATUS.md` with this structure (Markdown):

       ```markdown
       # Phase 2 Rollout Status

       **Run:** {timestamp from `date -Iseconds`}
       **Wrapper:** /home/btc/github/cos-docs/scripts/scaffold-all.sh
       **Per-repo log dir:** {LOG_DIR from wrapper output}

       ## Summary

       | Status | Count |
       |--------|-------|
       | OK     | N     |
       | SKIP   | N     |
       | FAIL   | N     |
       | TOTAL  | 30    |

       ## Per-Repo Detail

       | Repo | Status | Reason | Remediation |
       |------|--------|--------|-------------|
       | bis-forge | SKIP | dirty working tree | User to commit/stash/clean, then re-run `scaffold.sh /home/btc/github/bis-forge` |
       | ... | ... | ... | ... |

       ## Failure Triage Notes

       (For each FAIL row, state what was investigated and what action was taken — see Task 2.)

       ## PACKAGE_OVERRIDES Additions

       (If new module-name mismatches were discovered during the sweep, list them here so they can be backported into scaffold-all.sh and re-run for those specific repos.)
       ```

    3. **Triage each FAIL and SKIP** — for every non-OK row:

       - **SKIP (branch ≠ main/master):** Note in remediation. Do NOT auto-switch. User decides per-repo whether to checkout main/master (they may have uncommitted WIP elsewhere). Per 02-RESEARCH.md inventory, expect: `coinbase_websocket_BTC_pricefeed` (kubernetes), `COS-Capability-Gated-Agent-Architecture` (spec-decomposition-extraction), `COS-electrs` (excluded anyway), `OrbWeaver` (kubernetes), `quant-dashboard` (kubernetes), `COS-CIE` (master — should NOT skip; verify wrapper accepts master), `COS-Infra` (master — same), `COS-LangGraph` (master — same).

       - **SKIP (dirty tree):** Note remediation. Per 02-RESEARCH.md inventory, expect ~14 dirty repos. For each, check `git -C /home/btc/github/<repo> status --short` and record what the changes are. If they appear safe (e.g., uncommitted edits to unrelated files), present the user with a recommendation: "stash or commit these N files, then re-run". Do NOT auto-stash.

       - **FAIL (scaffold):** Read `${LOG_DIR}/${repo}.scaffold.log`. Common: `pyproject.toml` parse fails because the file is not present (e.g., COS-electrs Cargo-only — but it's excluded anyway). Document the root cause.

       - **FAIL (mkdocs build --strict):** Read `${LOG_DIR}/${repo}.build.log`. Look for `Could not collect '<module>'` warnings (Pitfall 4: module-name mismatch). For each such warning, run:
         ```bash
         python3 -c "import sys; sys.path.insert(0, '/home/btc/github/<repo>/src'); import <guess>" 2&gt;&amp;1 | head -5
         ```
         OR `ls /home/btc/github/<repo>/src/` to discover the actual top-level module. Add the discovered name to the report's "PACKAGE_OVERRIDES Additions" section. (Backporting into scaffold-all.sh is in Task 2.)

       - **FAIL (git commit):** Should be rare given preflight already gated dirty trees. Read the log; usually pre-commit hook or git config issue.

    4. **Re-run for fixable repos** (after triage):
       - For each repo where the user resolves the dirty tree (manual cleanup): re-run `scaffold.sh /home/btc/github/<repo>` (single-repo, not the wrapper) and update its row to OK + note "manually scaffolded after dirty-tree resolution".
       - For each repo with a new PACKAGE_OVERRIDES entry needed: defer the re-run to AFTER Task 2 backports the override into scaffold-all.sh; then re-run via `scaffold.sh --package <module> /home/btc/github/<repo>`. Update the row to OK.

    5. **Final summary line in the status report** must show: `OK: N` and `STILL_PENDING: M` where M is the count of repos still SKIP/FAIL after all triage. Phase 2 success criteria #1 and #5 require N ≥ 28 (allow up to 2 documented exclusions for genuine blockers — but note that documented skips do NOT satisfy CONT-01; the user must commit to either resolving them or explicitly de-scoping them from v1).
  </action>
  <verify>
    <automated>test -f /home/btc/github/cos-docs/.planning/phases/02-content-migration/02-ROLLOUT-STATUS.md &amp;&amp; grep -q '| Repo |' /home/btc/github/cos-docs/.planning/phases/02-content-migration/02-ROLLOUT-STATUS.md &amp;&amp; grep -q -E 'OK|SKIP|FAIL' /home/btc/github/cos-docs/.planning/phases/02-content-migration/02-ROLLOUT-STATUS.md &amp;&amp; grep -q 'TOTAL' /home/btc/github/cos-docs/.planning/phases/02-content-migration/02-ROLLOUT-STATUS.md &amp;&amp; OK_COUNT=$(grep -cE '\| OK \|' /home/btc/github/cos-docs/.planning/phases/02-content-migration/02-ROLLOUT-STATUS.md || echo 0) &amp;&amp; echo "OK rows: $OK_COUNT (target ≥ 25)" &amp;&amp; for r in COS-Core COS-Bitcoin-Protocol-Intelligence-Platform COS-LangGraph; do if [ -d /home/btc/github/$r/.git ]; then git -C /home/btc/github/$r log --oneline -1 | grep -q 'cos-docs scaffold' &amp;&amp; echo "$r: OK" || echo "$r: NO_COMMIT"; fi; done</automated>
  </verify>
  <done>
    - 02-ROLLOUT-STATUS.md exists with per-repo table covering all 30 in-scope repos
    - At minimum 25 of 30 repos show OK status (allows up to 5 documented blockers; aim for ≥28)
    - Every non-OK row has both a Reason and a Remediation column populated
    - For each successful repo R: `git -C /home/btc/github/R log --oneline -1` shows the scaffold commit on main or master
    - For each successful Python repo R: `mkdocs build --strict` was confirmed passing in the wrapper smoke gate (recorded in build log)
    - PACKAGE_OVERRIDES Additions section lists any module-name mismatches discovered beyond COS-LangGraph (may be empty if none found)
  </done>
</task>

<task type="auto">
  <name>Task 2: Backport new PACKAGE_OVERRIDES into scaffold-all.sh + amend REQUIREMENTS.md DIAG-02 + update CLAUDE.md project map</name>
  <files>
    /home/btc/github/cos-docs/scripts/scaffold-all.sh
    /home/btc/github/cos-docs/.planning/REQUIREMENTS.md
    /home/btc/github/CLAUDE.md
  </files>
  <read_first>
    - /home/btc/github/cos-docs/.planning/phases/02-content-migration/02-ROLLOUT-STATUS.md (the report from Task 1 — its PACKAGE_OVERRIDES Additions section)
    - /home/btc/github/cos-docs/scripts/scaffold-all.sh (current state of PACKAGE_OVERRIDES map)
    - /home/btc/github/cos-docs/.planning/REQUIREMENTS.md lines ~31-34 (DIAG-02 current wording)
    - /home/btc/github/CLAUDE.md "## Project Map" table (the stale project map per D-14)
    - /home/btc/github/cos-docs/.planning/phases/02-content-migration/02-CONTEXT.md (D-08 amendment text, D-09 exempt list, D-14 reconciliation requirements)
    - /home/btc/github/cos-docs/.planning/phases/02-content-migration/02-RESEARCH.md §"Reconciliation Summary" (the renames + missing repos lists)
  </read_first>
  <action>
    **Step A — Backport PACKAGE_OVERRIDES additions to scaffold-all.sh:**

    Read the "PACKAGE_OVERRIDES Additions" section of 02-ROLLOUT-STATUS.md. For each `<repo>=<module>` entry there:
    1. Add to `declare -A PACKAGE_OVERRIDES=( ... )` block in `/home/btc/github/cos-docs/scripts/scaffold-all.sh`.
    2. Re-run `scaffold.sh --package <module> /home/btc/github/<repo>` for that single repo (the per-repo invocation, NOT the full wrapper sweep).
    3. Verify `mkdocs build --strict` now passes for that repo (manually, in its venv).
    4. Update the row in 02-ROLLOUT-STATUS.md from FAIL → OK with note "resolved via PACKAGE_OVERRIDES backport".
    5. If git tree changed (api.md updated), amend or append commit per D-02.

    If no additions were discovered (PACKAGE_OVERRIDES Additions section is empty), skip Step A.

    **Step B — Amend REQUIREMENTS.md DIAG-02 wording (D-08):**

    Find the current line in `/home/btc/github/cos-docs/.planning/REQUIREMENTS.md` (around line 33):
    ```
    - [ ] **DIAG-02**: At least one Mermaid architecture diagram exists per repo (in `docs/architecture.md`)
    ```

    Replace it with (verbatim, per 02-RESEARCH.md §"DIAG-02 Amendment Mechanics"):
    ```
    - [ ] **DIAG-02**: At least one Mermaid architecture diagram exists per non-exempt repo (in `docs/architecture.md`). Exempt repos (no software architecture to diagram): `COS-Hardware`, `COS-Network`, `COS-Capability-Gated-Agent-Architecture`. Exempt list maintained in `cos-docs/scripts/scaffold-all.sh` (DIAGRAM_EXEMPT array); additions during Phase 2 authoring update both that array and this requirement footnote.
    ```

    Do NOT change DIAG-02's traceability table row (still maps to Phase 2). Do NOT renumber.

    **Step C — Update /home/btc/github/CLAUDE.md project map (D-14):**

    The current project map table in `/home/btc/github/CLAUDE.md` lists ~25 repos and is stale relative to disk. Apply these specific edits per 02-RESEARCH.md §"Reconciliation Summary":

    **Renames** (update existing rows):
    - `fred-forge` → `FRED-Forge`
    - `edgar-forge` → `EDGAR-Forge`
    - `bitcoin_node` → `COS-BTC-Node` (and update purpose text accordingly — was "Bitcoin Core node config, Dockerfile, and K8s deploy scripts"; verify this still describes COS-BTC-Node by reading its CLAUDE.md)

    **Additions** (new rows in the Project Map table):
    - `COS-MSE` | Python | Market Sentiment Engine
    - `cos-data-access` | Python | (read its README/CLAUDE.md for one-line purpose)
    - `cos-signal-explorer` | Python | (read its README/CLAUDE.md for one-line purpose)
    - `cos-webpage` | TS | (read its README/CLAUDE.md for one-line purpose)
    - `COS-Infra` | Docs/Config | Workspace deployment guides
    - `stooq-forge` | Python | Stooq financial data downloader

    **Exclusion note** (add as a footnote below the Project Map table, NOT as a row):
    ```
    > **Note:** `COS-electrs` (Rust Electrum server) lives in this workspace but is excluded from the cos-docs aggregator (Phase 2 D-14) — its Cargo-only build does not match the Python/TS docs pipeline. Revisit if Rust API rendering becomes a priority.
    >
    > **Note:** A lowercase duplicate `capability-gated-agent-architecture` directory exists alongside `COS-Capability-Gated-Agent-Architecture`; only the PascalCase version is in scope (Phase 2 D-12).
    ```

    Total project map after edits: 30 in-scope repos + 1 footnoted exclusion (COS-electrs) = 31 disk repos accounted for. Verify by counting table rows.

    **DO NOT** add `cos-docs` itself to the project map (it's the aggregator; the table describes content sources, not the aggregator).
  </action>
  <verify>
    <automated>grep -q 'non-exempt repo' /home/btc/github/cos-docs/.planning/REQUIREMENTS.md &amp;&amp; grep -q 'COS-Hardware.*COS-Network.*COS-Capability-Gated-Agent-Architecture\|COS-Hardware`, `COS-Network`, `COS-Capability-Gated-Agent-Architecture' /home/btc/github/cos-docs/.planning/REQUIREMENTS.md &amp;&amp; grep -q 'FRED-Forge' /home/btc/github/CLAUDE.md &amp;&amp; grep -q 'EDGAR-Forge' /home/btc/github/CLAUDE.md &amp;&amp; grep -q 'COS-BTC-Node' /home/btc/github/CLAUDE.md &amp;&amp; grep -q 'COS-MSE' /home/btc/github/CLAUDE.md &amp;&amp; grep -q 'COS-Infra' /home/btc/github/CLAUDE.md &amp;&amp; grep -q 'stooq-forge' /home/btc/github/CLAUDE.md &amp;&amp; grep -q 'cos-data-access' /home/btc/github/CLAUDE.md &amp;&amp; grep -q 'cos-signal-explorer' /home/btc/github/CLAUDE.md &amp;&amp; grep -q 'cos-webpage' /home/btc/github/CLAUDE.md &amp;&amp; grep -q 'COS-electrs' /home/btc/github/CLAUDE.md &amp;&amp; ! grep -E '^\| `fred-forge`' /home/btc/github/CLAUDE.md &amp;&amp; ! grep -E '^\| `edgar-forge`' /home/btc/github/CLAUDE.md &amp;&amp; ! grep -E '^\| `bitcoin_node`' /home/btc/github/CLAUDE.md &amp;&amp; bash -n /home/btc/github/cos-docs/scripts/scaffold-all.sh &amp;&amp; echo OK</automated>
  </verify>
  <done>
    - REQUIREMENTS.md DIAG-02 wording updated to "per non-exempt repo" with the 3 D-09 repos named inline
    - /home/btc/github/CLAUDE.md project map: stale entries renamed (fred-forge→FRED-Forge, edgar-forge→EDGAR-Forge, bitcoin_node→COS-BTC-Node), 6 new rows added (COS-MSE, cos-data-access, cos-signal-explorer, cos-webpage, COS-Infra, stooq-forge), COS-electrs documented as out-of-stack via footnote
    - Project map row count: 30 in-scope (matches D-14)
    - scaffold-all.sh PACKAGE_OVERRIDES backported with any new mismatches discovered in Task 1; bash syntax still valid
    - CLAUDE.md is in /home/btc/github/, NOT a cos-docs file — commit happens in the workspace-CLAUDE.md repo (which may be cos-docs or a parent; verify with `git -C /home/btc/github status` whether it is tracked, and if so commit there with message `docs: reconcile workspace project map with disk (cos-docs phase 2 D-14)`)
  </done>
</task>

</tasks>

<verification>
**Plan-level checks:**

1. `grep -cE '\| OK \|' /home/btc/github/cos-docs/.planning/phases/02-content-migration/02-ROLLOUT-STATUS.md` ≥ 25
2. `grep -q 'non-exempt' /home/btc/github/cos-docs/.planning/REQUIREMENTS.md` (DIAG-02 amendment present)
3. `grep -q 'FRED-Forge' /home/btc/github/CLAUDE.md && grep -q 'COS-MSE' /home/btc/github/CLAUDE.md` (CLAUDE.md updates present)
4. `for r in $(grep -oE '^\| [a-zA-Z][^ ]*' /home/btc/github/cos-docs/.planning/phases/02-content-migration/02-ROLLOUT-STATUS.md | tr -d '|' | xargs); do test -f /home/btc/github/$r/mkdocs.yml && test -f /home/btc/github/$r/docs/index.md && echo "$r: scaffolded" || echo "$r: not scaffolded (check status report)"; done`
5. For Python repos that are OK: `test -f /home/btc/github/<repo>/docs/api.md && grep -q '^::: ' /home/btc/github/<repo>/docs/api.md`

**Anti-regression:**
- COS-Core's existing scaffold from Phase 1 verification still works: `cd /home/btc/github/COS-Core && .venv-docs/bin/mkdocs build --strict` exits 0 (or re-create the venv via the wrapper steps and verify the same)
</verification>

<success_criteria>
- All 30 in-scope sibling repos either committed (OK) OR documented in the rollout status with reason+remediation (SKIP/FAIL)
- ≥ 25 of 30 repos show OK in 02-ROLLOUT-STATUS.md (target ≥ 28; lower bound accommodates the heaviest-dirty repos that need user intervention)
- REQUIREMENTS.md DIAG-02 wording amended per D-08
- /home/btc/github/CLAUDE.md project map matches disk reality per D-14 (renames + additions + COS-electrs footnote)
- Any newly discovered PACKAGE_OVERRIDES backported into scaffold-all.sh and verified with single-repo re-run
- Per CONT-01: ROLLOUT-STATUS.md serves as the auditable record of "which of the ~25/30 sibling repos are scaffolded"
- Per DIAG-02 (post-amendment): only non-exempt repos owe a Mermaid diagram (delivered in 02-03)
</success_criteria>

<output>
After completion, create `/home/btc/github/cos-docs/.planning/phases/02-content-migration/02-02-SUMMARY.md` documenting:
- Final OK/SKIP/FAIL counts
- List of any repos NOT scaffolded with explanation
- All PACKAGE_OVERRIDES additions backported
- Diff summary of REQUIREMENTS.md and CLAUDE.md edits
- Any deviations from D-14's "30 repos in scope" (e.g., if user de-scoped a repo mid-rollout)
</output>
</content>
</invoke>